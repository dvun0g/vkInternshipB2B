export const SELECT_TOWER = ['А', 'Б'];
export const SELECT_FLOOR = new Array(27)
	.fill(0)
	.map((_, i) => `Этаж - ${i + 1}`);
export const SELECT_MEETING_ROOM_NUMBER = new Array(10)
	.fill(0)
	.map((_, i) => `Переговорная - ${i + 1}`);
